# Base URL for the REST API (Node/Express backend, built separately).
# axiosInstance.js falls back to "/api" if this is unset — pair that with
# a Vite dev-server proxy, or set the full origin here, e.g.:
# VITE_API_BASE_URL=http://localhost:4000/api
VITE_API_BASE_URL=/api
